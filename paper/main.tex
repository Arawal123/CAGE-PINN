\documentclass[review,12pt]{elsarticle}
\usepackage{amsmath,amssymb,booktabs,graphicx,hyperref}

\journal{Journal of Computational Physics}

\begin{document}

\begin{frontmatter}
\title{Cross-fitted Audit-Guided Enforcement for Compute-Budgeted Generalization in Physics-Informed Neural Networks}
\author{Authors withheld for draft}

\begin{abstract}
\textbf{RESULTS\_REQUIRED.} This abstract must be written only after the
confirmatory manifest is frozen and the claim-evidence ledger approves every
numerical sentence.
\end{abstract}
\end{frontmatter}

\section{Introduction}
Physics-informed neural networks may attain low residual loss on finite
collocation sets while retaining large off-grid violations. This paper asks
whether unseen physics residuals can guide enforcement without reference
solutions. Novelty language remains conditional on completion of the systematic
search in the repository.

\section{Related work}
The final text will follow the machine-readable taxonomy and collision matrix.
The residual-slope and weak-form atoms are treated as established primitives.

\section{Method}
Let \(A_t^{(s)},A_t^{(m)},A_t^{(c)}\) denote selector, monitor, and calibration
folds. The selector chooses compute allocation but never supplies a direct
parameter update; the monitor affects neither allocation nor optimization.
Full notation and theory scope are maintained in \texttt{docs/method.md}.

\section{Theory}
Only bounded-score fixed-schedule concentration and conditional first-order
utility statements are planned. Residual-to-solution claims require explicit
problem stability assumptions. No generic guarantee is made for shocks or
non-coercive systems.

\section{Equal-compute protocol}
The primary comparison is equal end-to-end wall time, with calibrated AD tokens
as a key secondary view. All audit and controller overhead is debited.

\section{Results}
\textbf{RESULTS\_REQUIRED.} Tables and figures must be generated from immutable
raw records. Smoke diagnostics are not admissible evidence.

\section{Ablations and sensitivity}
\textbf{RESULTS\_REQUIRED.} Use the preregistered ablation manifest; remove
unsupported components rather than hiding negative evidence.

\section{Limitations}
The audit score is not solution error. Its usefulness depends on sampling,
stability, and transfer from selector to monitor. High-order AD and weak
quadrature can dominate cost. Publication novelty remains unapproved.

\section{Reproducibility}
Code, manifests, environment fingerprints, raw records, and claim evidence are
versioned in the accompanying repository.

\appendix
\section{PDE definitions and references}
Generated from benchmark metadata after reference validation.
\section{Per-seed results}
\textbf{RESULTS\_REQUIRED.}

\end{document}

